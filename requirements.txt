# Python dependencies for fertilizer_market refresh + scrapers.
# Used by:
#   scripts/refresh.py                     (canonical refresh — CI runs this on ubuntu-latest)
#   scripts/refresh_nbs.py                 (PIB NBS press-release scraper)
#   scripts/Refresh-FertilizerMarket.ps1   (legacy pwsh runner — manual workstation fallback)
#   scripts/Refresh-IndiaNBS.ps1           (legacy pwsh runner — manual workstation fallback)
#
# Minimum-version pins.

pandas>=2.0
pyarrow>=14.0
openpyxl>=3.1
pycountry>=23.12
beautifulsoup4>=4.12
lxml>=5.0
requests>=2.31
pytest>=8.0
